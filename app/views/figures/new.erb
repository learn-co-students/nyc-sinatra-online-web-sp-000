<h1>Create a New Figure of History </h1>

<form action="/figures" method="post">
  <label for="name">Name</label>
  <input type="text" name="figure[name]" id="figure_name"/>

  <h3>Titles:</h3>
  <% Figure.all.each do |f| %>
  <label><%= f.name %></label>
  <input type="checkbox" id="<%= f.name %>" name="figure[title_ids][]"/><br>
  <% end %>

  <h3>Create a New Title</h3>
  <label>Name:</label>
  <input type="text" name="title[name]" id="new_title"/>

  <h3>Landmarks</h3>
  <% Landmark.all.each do |l| %>
  <label><%= l.name %></label>
  <input type="checkbox" id="<%= l.name %>" name="figure[landmark_ids][]"/><br>
  <% end %>

  <h3>Create a New Landmark:</h3>
  <label>Name:</label>
  <input type="text" name="landmark[name]" id="new_landmark"/>

  <button type="submit">Create New Figure</button>
</form>
