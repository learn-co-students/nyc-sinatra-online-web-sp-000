<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>New Figure</title>
  </head>
  <body>
    <h1>Create a new figure:</h1>

    <form action="/figures" method="post">
      <label>Name:</label>
      <input type="text" id="figure_name" name="figure[name]" value="name">

      </br>
      </br>
      <label>Their titles:</label>
      <%Title.all.each do |t|%>
        <input type="checkbox" name="figure[title_ids][]" id="title_<%=t.id%>" value="title_<%=t.id%>"><%=t.name%></input>
      <%end%>
      <br></br>

      <label>or make a new title:</label>
      <input type="text" name="title[name]" id="new_title">
      <br></br>

      <label>Select landmarks:</label>
      <%Landmark.all.each do |l|%>
        <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%=l.id%>" value="landmark_<%=l.id%>"><%=l.name%></input>
      <%end%>
      </br>

      <label>Or, make a new landmark</label>
      <input type="text" name="landmark[name]" id="new_landmark" value="landmark">
      </br>
      </br>

      <input type="submit" id="submit" value="Create New Figure">
    </form>


  </body>
</html>
