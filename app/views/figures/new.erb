<!DOCTYPE html>
<html>
  <head>
<h1>Create a New Figure</h1>
  </head>
  <body>
    <form method="POST" action="/figures">
      <label><h4>Name:</h4></label><input type="text" name="figure[name]" id="figure_name">

      <label><h4>Title(s)</h4></label>
      <em><strong>Choose from existing titles:</strong></em><br><br>
      <% @titles.each do |title| %>
        <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id%>">
        <%= title.name %>
        <br><br>
        <% end %>
        <br>
      <em><strong>and/or create a new Title:</strong></em><input type="text" name="title[name]" id="new_title">

      <label><h4>Landmark(s)</h4></label>
      <em><strong>Choose from existing Landmarks:</strong></em><br><br>
      <% @landmarks.each do |landmark| %>
        <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id%>">
        <%= landmark.name %>
        <br><br>
        <% end %>
        <br>
      <em><strong>and/or create a new Landmark:</strong></em><input type="text" name="landmark[name]" id="new_landmark">

      <input type="submit" value="Create New Figure">
    </form>
  </body>

</html>
