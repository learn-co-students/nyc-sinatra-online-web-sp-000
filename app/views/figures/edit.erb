<!DOCTYPE html>
<html lang="en">
<head>
	<title>Edit</title>
</head>
<body>
	<h1>Edit <%= @figure.name %></h1>

	<form action="/figures/<%= @figure.id %>" method="post">
		<input id="hidden" type="hidden" name="_method" value="patch">

		<label for="figure_name">Name:</label>
		<input id="figure_name" type="text" name="figure[name]" value="<%= @figure.name %>">
		<br><br>

		<label for="new_title">New Title:</label>
		<input id="new_title" type="text" name="title[name]">
		<br><br>

		<label>Existing titles:</label>
		<br>
		<% Title.all.each do |title| %>
		<input id="<%= title.id %>" type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" <%= 'checked' if @figure.titles.include?(title) %>> <%= title.name %>
		<br>
		<% end %>
		<br>

		<label for="new_landmark">New landmark:</label>
		<input id="new_landmark" type="text" name="landmark[name]">

		<label for="year_completed">Year Completed:</label>
		<input id="year_completed" type="text" name="landmark[year_completed]">
		<br><br>

		<label>Existing landmarks:</label>
		<br>
		<% Landmark.all.each do |landmark| %>
			<input id="<%= landmark.id %>" type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%= 'checked' if @figure.landmarks.include?(landmark) %>> <%= landmark.name %>
			<br>
		<% end %>
		<br>

		<input type="submit" value="Edit Figure">


	</form>
</body>
</html>
