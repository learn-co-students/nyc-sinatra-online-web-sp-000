<!DOCTYPE html>
<html>
  <head>
    <title>Figures</title>
  </head>

  <body>
    <h2> Edit the Figure - <%= @figure.name %></h2>

    <form action="/figures/<%= @figure.id %>" method="POST">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <label>Edit the Figure's Name: </label><input id="figure_name" type="text" name="figure[name]" value="<%= @figure.name %>">
      <br><br>

      <label>Edit the Titles for <%= @figure.name %></label><br>
      <% @titles.each do |title| %>
        <input type="checkbox" id="title_<%= title.id %>" name="figure[title_ids][]" value="<%= title.id %>" <%='checked' if @figure.titles.include?(title) %>><%= title.name %></input><br>
      <% end %><br>

      <label>Edit the Landmarks for <%= @figure.name %></label><br>
      <% @landmarks.each do |landmark| %>
        <input type="checkbox" id="landmark_<%= landmark.id %>" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%='checked' if @figure.landmarks.include?(landmark) %>><%= landmark.name %></input><br>
      <% end %>

      <h4>Create New Landmark</h4>
      <label>Name: </label></label><input id="new_landmark" type="text" name="landmark[name]">
      <label>Year: </label></label><input id="new_year" type="text" name="landmark[year]"><br><br>

      <br><input id="Save" type="submit" value="Edit Figure">
      </form>
  </body>
</html>
