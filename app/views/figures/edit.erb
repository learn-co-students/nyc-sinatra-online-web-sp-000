<!DOCTYPE html>
<html>
<head>
    <title>Edit <%= @figure.name %></title>
<head
<body>
    <form action="/figures/<%= @figure.id %>" method="post">
        <input id="hidden" type="hidden" name="_method" value="patch">
        <label> Edit <%= @figure.name %></label><br>
        <br>
        Name:<input type="text" id="figure_name" name="figure[name]" value="<%= @figure.name %>"><br>
        <br>
        <label>Edit Titles</label><br>
        <br>
        <% @titles.each do |title| %>
            <input id="title_<%= title.id %>" type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" <%='checked' if @figure.titles.include?(title) %>><%= title.name %><br>
        <% end %>
        and/or Create a New Title:<br>
            <label>Title:</label><input type="text" name="title[name]" id="new_title">
        <br><br>
        <br>
        <b>Edit Landmarks:</b><br>
        <% @landmarks.each do |landmark| %>
            <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>" <%='checked' if @figure.landmarks.include?(landmark) %>>
            <%= landmark.name %>
            <br>
          <% end %>
          and/or Create a New Title:<br>
            Name:<input id="new_landmark" type="text" name="landmark[name]"><br>
            Year:<input id="landmark_year" type="text" name="landmark[year]"><br>
        <input type="submit" value="Edit Figure">
    </form>
</body>
</html>
<html>